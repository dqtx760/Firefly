---
title: "一个Skill 让 Agent 自动操作浏览器，告别重复枯燥任务"
url: "https://www.bilibili.com/video/BV1aDKb6pEnJ/"
bvid: "BV1aDKb6pEnJ"
cid: "40155549129"
author: "技术爬爬虾"
upload_date: "2026-07-21"
subtitle_lang: "中文"
created: "2026-07-21"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116956640314668&bvid=BV1aDKb6pEnJ&cid=40155549129&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

本期视频我们使用的工具是ego lite 还有配套的Skill。 ego lite 是一个基于 Chromium内核开发的全新浏览器，核心设计理念是让Agent和人类能共用同一个Chrome进行流畅协作。 ego lite通过全新的架构设计，针对传统浏览器自动化工具里： 反复登录 ，内存占用大 ，Token消耗高， 执行速度慢等几大痛点进行了明显优化。

ego lite skill：https://github.com/citrolabs/ego-lite
ego lite 官网：https://lite.ego.app/

## 章节

- `00:00` 引言
- `00:58` 基础使用
- `02:37` 技术原理
- `05:26` 进阶实战

## 字幕

### 引言 `00:00`

`00:00` 这个skill能给任意的AI agent接入顶级的浏览器
`00:03` 自动化能力
`00:04` 帮你完成各种机械重复的操作
`00:07` 本期视频我们使用的工具是ego light
`00:09` 还有配套的skill
`00:11` Ego light
`00:11` 是一个基于与chromium内核开发的全新浏览器
`00:15` 核心设计理念是让agent和人类能共用同一个chrome
`00:19` 进行流畅协作
`00:20` ego light通过全新的架构设计
`00:22` 针对传统浏览器自动化工具里面反复登录
`00:26` 内存占用大
`00:27` token消耗高
`00:28` 执行速度慢等几大痛点都进行了明显优化
`00:31` 本期视频带来一个手把手的完整教程
`00:34` 我们看看怎么在cloud code
`00:36` work body等各种AI agent里面接入ego li
`00:40` 熟练使用以后
`00:41` 我们还能把很多工作流程沉淀成可复用的skill
`00:44` 让AI能够又快又省的完成任务
`00:47` 甚至还有许多固定流程
`00:49` 只需要让AI编写好一个脚本
`00:51` 就能全程脱离AI参与0token全自动完成工作
`00:55` 好废话不多说
`00:56` 我们直接开始
`00:57` 我们先去ego light官网把浏览器下载一下

### 基础使用 `00:58`

`01:01` 目前上线的是Mac版本
`01:03` 完全的免费使用
`01:04` 下载完成以后
`01:05` 打开安装文件
`01:06` 拖拽进来就完成了
`01:07` 安装ego light第一次启动的时候
`01:10` 会询问我是否导入之前的浏览器配置
`01:13` 这里我选择chrome导入以后
`01:15` 我的chrome上所有的网站登录状态
`01:17` cookie插件
`01:18` profile等数据都迁移到了ego li
`01:21` 这些数据都是离线保存在本地设备上的
`01:24` 后续AI的自动化操作都可以复用
`01:27` 这些登录太彻底
`01:28` 解决了传统浏览器自动化的痛点之一
`01:31` 也就是重复登录的问题
`01:33` 接下来我们来测试一下
`01:35` 这里我先用cloud code接入
`01:36` 打开终端
`01:37` 输入cloud来启动cloud code
`01:39` 进来以后
`01:40` 我们可以直接敲斜线
`01:41` Ego browser
`01:42` 调用ego light的skill
`01:44` ego light第一次启动的时候
`01:46` 会自动扫描电脑上已经安装的agent
`01:49` 自动把skill存放到agent的对应目录里面
`01:52` 零配置就能使用它的skill
`01:54` 非常的方便
`01:55` 滑把虾
`01:55` 最近经常让chat gbt帮我画一些信息图
`01:59` 作为补充材料
`02:00` 插到视频里面去
`02:01` 用传统方法需要打开chat gbt的对话框
`02:04` 点击创建图片
`02:05` 然后填入提示词
`02:07` 等待图片生成
`02:08` 最后下载这套流程至少占用两到3分钟
`02:11` 现在就可以直接交给AI去做了
`02:13` 我输入提示词
`02:15` 进入chat gbt的网页端
`02:17` 选择生成图片功能
`02:18` 16比九的信息图
`02:20` 深色背景展示传统浏览器自动化的痛点
`02:23` 等待图片生成完毕以后
`02:25` 把图片下载到桌面
`02:26` 并且重新命名为时间下划线图片主题
`02:30` 接下来AI就开始工作了
`02:32` 我们可以随时监控他的工作过程
`02:34` ego li右上角有一个数字的小图标

### 技术原理 `02:37`

`02:37` 点击就展开了它的核心工作页面space
`02:40` 每个space都是一个独立的工作空间
`02:44` 其中蓝色的就是agent正在控制的
`02:46` 其余的则是我
`02:47` 也就是用户正在控制的agent
`02:49` 可以同时创建多个space
`02:51` 在后台并行执行任务
`02:54` 与此同时
`02:54` 我依然可以正常浏览网页
`02:56` 双方互不干扰
`02:57` 之前我使用的playwright pop tier
`03:00` 这种传统浏览器自动化工具
`03:02` 如果想让多个agent并行工作
`03:04` 每个任务通常会需要启动一个
`03:06` 独立的浏览器实例
`03:08` 并且复制一份profile
`03:10` 因此会产生大量的内存和进程开销
`03:13` space架构就很好地解决了内存占用量大的痛点
`03:16` 根据官方文档的数据
`03:18` 相比
`03:18` 传统方案
`03:19` 它可以将内存跟进程的开销降低几十倍
`03:22` 我们再来看一下cloud code里面的执行过程
`03:25` cloud code驱动浏览器的时候
`03:27` 把所有的操作判断
`03:28` 输入输出等全部写在了一段node js的hair dock里面
`03:33` 然后发送给浏览器一次
`03:34` 执行这段代码还能进行逻辑判断
`03:37` 数组处理等等
`03:38` 并且进行多次的工具调用
`03:40` 比如这段AI操作浏览器填入画图指令
`03:44` 然后等待一秒再点击发送两个动作
`03:47` 只用一次交互就搞定了
`03:48` 这样有效降低了模型与浏览器之间的交互次数
`03:52` 任务完成的更快
`03:53` token消耗也更少
`03:55` 我们继续任务
`03:55` 过了一会儿
`03:56` 图片生成好了
`03:57` AI也帮我放到了桌面上面
`03:59` 在这个例子里面可以看到
`04:01` 得益于全新的架构设计
`04:03` ego light在登陆状态
`04:05` 保持资源占用
`04:06` token消耗和执行速度方面提升明显
`04:09` 实际表现很不错
`04:10` 接下来我们可以把这个过程再固化成skill
`04:14` 这里输入提示词
`04:15` 创建一个新的skill
`04:16` 把刚才操作的全过程
`04:18` 还有遇到的问题都提炼出来
`04:20` 保存到skill里面
`04:22` 后面我只要让你生成图片
`04:23` 你就可以调用这个skill完成AI
`04:26` 帮我们创建了skill
`04:27` 把这个任务里面可以复用的知识都提炼了进去
`04:30` 后续我只需要调用这个生图skill
`04:33` 就能又快又好的完成任务
`04:35` 我们来测试一下
`04:36` 这里我输入斜线
`04:37` 调用刚才创建的技能
`04:39` 再画张图来试一试
`04:41` 我们看到skill跟ego light配合起来
`04:44` 效率极高
`04:45` 写了一大段控制命令
`04:46` 只用了一次交互就完成了
`04:48` 从打开页面到点击发送按钮之间所有的操作
`04:52` 我们再来对比一下最后的token开销
`04:54` 左边是第一次让am摸索自己试错的开销
`04:58` 右边则是有了skill加持
`04:59` 下面的token开销
`05:01` skill跟eagle light配合起来使用非常不错
`05:04` 有了将近四倍的效率提升
`05:06` 我们再通过一个例子
`05:08` 看看ego light配合skill效率有多高
`05:11` 还是调用刚才创建的skill
`05:13` 开启多个space
`05:14` 画十张图给我选
`05:15` 主题如下
`05:16` 我们看到在ego light里面
`05:18` AI同时开启了十个space并行画图
`05:22` 我们只需要等待AI工作完成以后
`05:24` 选择一个最喜欢的图片就可以了

### 进阶实战 `05:26`

`05:26` 一次性把画图的效率提高了十倍
`05:29` 非常的棒
`05:30` 我们来看一个复杂一些的例子
`05:32` 最近沃克巴蒂的热度很高
`05:34` 很多人把重复的办公任务交给沃克巴蒂来做
`05:37` 取得了不错的效果
`05:39` 接下来我们就给work body安装上ego browser skill
`05:42` 让它解锁浏览器自动化的能力
`05:45` 让办公效率更上一个台阶
`05:47` 在work巴蒂左侧边栏找到专家技能连接器
`05:54` 添加ego browser技能
`05:56` skill安装好了
`05:57` 我们用一个办公任务来实测一下
`05:59` 比如现在我是一个电商运营岗的人员
`06:01` 我现在需要采集竞品商品的评论
`06:04` 挖掘用户最关心的需求点
`06:06` 我们先调用ego brother skill
`06:09` 查看这个商品的评论区
`06:10` 然后按时间倒序排序
`06:13` 抓取前100条评论
`06:14` 把所有的评论保存到一个CSV文件里面
`06:17` 开始之前记得打开允许完全访问A
`06:20` 在ego light里面打开了一个全新的space
`06:23` 来处理这个工作
`06:24` 他找到了商品的评论
`06:26` 然后按时间倒序排序
`06:28` 并且不不停的滚动来刷出更多的评论
`06:30` 过了一会儿
`06:31` 沃克巴蒂成功完成了任务
`06:33` 抓取到了100条数据
`06:34` 并且保存到了CSV文件里面
`06:37` 我们注意到抓取评论是一个固定的流程
`06:40` 其实并不需要AI进行智能化的控制
`06:42` 我们可以直接把它编写成一个固定的脚本
`06:45` 这里输入提示词
`06:46` 你把刚才的所有过程汇总成一个shell脚本
`06:50` 脚本接收一个参数
`06:51` 也就是电商网站的链接
`06:53` 执行脚本就能获得前100条评论
`06:55` 并且保存到一个CSV文件里面
`06:58` 脚本写完
`06:58` 你先做一轮测试
`07:00` 很快work克body为我们编写完成了脚本
`07:02` 并且他已经自测通过了
`07:04` 我们打开看一下
`07:06` AA以编程的方式把刚才的步骤都固定下来了
`07:09` 在这个脚本里面用JS代码分步骤完成了任务
`07:12` 比如第一步打开网页
`07:14` 第二步打开评论抽屉
`07:16` 第三步按时间倒序排序
`07:18` 我们也来测试一下
`07:19` 打开一个命令行终端
`07:21` 填上脚本地址
`07:22` 后面接一个商品网址
`07:24` 然后回车执行
`07:25` 我们进入ego light
`07:26` 就可以看到脚本正在飞速的自动执行
`07:29` 不依赖任何的agent
`07:31` 0token
`07:32` 零成本就完成了任务
`07:33` 最后也输出了CSV文件
`07:35` 取得了相同的效果
`07:37` 非常的不错
`07:38` 借助ego light跟work body的组合能力
`07:40` 我们还可以发挥想象力
`07:42` 做一些更好玩更复杂的操作
`07:44` 我计划本月21日到25日从青岛出发
`07:48` 前往苏州跟厦门两地游玩
`07:50` 你搜索票务信息
`07:51` 然后结合种草攻略
`07:53` 还有这几日的天气给我出一个行程规划
`07:56` 可以开启多个space并行工作
`07:59` 提高效率
`08:00` 我们看到work body调用ego li
`08:02` 同时开启了三个space并行工作
`08:05` 一个查询票务信息
`08:07` 一个查询天气
`08:08` 还有一个查询种草攻略
`08:10` 最后为我们输出了行程规划
`08:12` 包含天气情况
`08:13` 应该怎么乘车
`08:14` 还有每一天的时间表
`08:16` 内容详实
`08:17` 效果非常不错
`08:18` 我们再来看一个例子
`08:19` 我想找java开发相关的工作
`08:21` 我最大的诉求就是公司离家近
`08:24` 请结合百度地图
`08:25` 还有两个主要的招聘网站
`08:27` 帮我搜索离家近的最适合的岗位
`08:29` 20个输出成CSV文件
`08:31` 给我挑选
`08:32` AI调用ego light
`08:34` 打开地图网站
`08:35` 先确认我家的位置
`08:36` 然后在招聘网站上面
`08:38` 找到了青岛java开发的相关岗位
`08:40` 接下来又调用用地图确认距离
`08:42` 最后输出了完整的CSV文件
`08:45` 我们打开就可以看到20个候选岗位
`08:47` 而且按照距离远近进行了排序
`08:49` 这些复杂的案例
`08:51` ego light都能很好的帮我完成
`08:53` 而且对比我之前用过的浏览器自动化工具
`08:56` token消耗更更低
`08:57` 执行速度更快好
`08:59` 本期视频就到这里
`09:00` 感谢大家
`09:01` 我们下期再见