---
title: 本周创建的笔记
published: 2025-05-01
tags:
  - dataview模板
category: Zen
draft: false
pinned: false
image:
---
### 最近7天创建的笔记
```dataview
TABLE WITHOUT ID
  dateformat(file.ctime, "yyyy/MM/dd") AS "日期",
  file.link AS "笔记",
  category AS "主题",
  join(file.tags, " ") AS "tags"
WHERE 
  contains(file.folder, "posts")
  AND file.ctime >= date(today) - dur(10 days)
SORT file.ctime DESC, category ASC
```
---

### 最近7天修改的笔记
```dataview
TABLE WITHOUT ID
  dateformat(file.ctime, "yyyy/MM/dd") AS "日期",
  file.link AS "笔记",
  category AS "主题",
  join(file.tags, " ") AS "tags"
WHERE 
  contains(file.folder, "posts")
  AND file.mtime >= date(today) - dur(7 days)
SORT file.mtime DESC, category ASC
```
