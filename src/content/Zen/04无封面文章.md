```dataview
TABLE WITHOUT ID
  file.link AS "笔记",
  join(file.tags, " ") AS "标签",
  image AS "封面"
WHERE 
  contains(file.folder, "posts")
  AND (image = null OR image = 0)
SORT file.ctime DESC
```
---

**补充说明**

 `image = null` 来判断「没有 `image` 字段 / 字段为空」
