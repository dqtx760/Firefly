---
import { getCollection } from "astro:content";
import MainGridLayout from "@layouts/MainGridLayout.astro";
import { Icon } from "astro-icon/components";
import LinkCard from "@/components/LinkCard.astro";

// 获取所有"实用导航"分类的文章
const navPosts = await getCollection("posts", ({ data }) => {
    return (import.meta.env.PROD ? data.draft !== true : true) && data.category === "实用导航";
});

// 按文章标题分组（去掉标题中的"导航"后缀）
interface NavCategory {
    name: string;
    posts: typeof navPosts;
}

const navCategories: NavCategory[] = navPosts.map(post => ({
    name: post.data.title.replace(/导航$/, ''),
    posts: [post]
})).sort((a, b) => a.name.localeCompare(b.name, 'zh-CN'));
---

<MainGridLayout title="友链">
    <div class="card-base p-6 md:p-8">
        <div class="flex items-center gap-2 mb-6">
            <div class="h-8 w-8 rounded-lg bg-[var(--primary)] flex items-center justify-center text-white dark:text-black/70">
                <Icon name="material-symbols:diversity-3" class="text-[1.5rem]">
            </div>
            <h1 class="text-2xl font-bold text-black dark:text-white">实用导航</h1>
        </div>

        <p class="text-sm text-black/60 dark:text-white/60 mb-6">
            精选实用网站推荐，涵盖网站搭建、人工智能、效率工具等多个分类
        </p>

        <div class="space-y-8">
            {navCategories.map((category) => (
                <div class="nav-category">
                    <div class="category-header">
                        <div class="flex items-center justify-center gap-2 mb-4">
                            <div class="w-8 h-px border-t border-dashed border-[var(--primary)]/50"></div>
                            <div class="relative w-5 h-5 rounded-lg bg-gradient-to-br from-[var(--primary)] to-[var(--primary)]/70 flex items-center justify-center overflow-hidden">
                                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-3 h-3 text-white dark:text-black/70 relative z-10">
                                    <rect x="3" y="3" width="7" height="7"></rect>
                                    <rect x="14" y="3" width="7" height="7"></rect>
                                    <rect x="14" y="14" width="7" height="7"></rect>
                                    <rect x="3" y="14" width="7" height="7"></rect>
                                </svg>
                                <div class="absolute inset-0 bg-gradient-to-tr from-white/20 to-transparent"></div>
                            </div>
                            <span class="font-bold" style="color: var(--primary)">{category.name}</span>
                            <div class="w-8 h-px border-t border-dashed border-[var(--primary)]/50"></div>
                        </div>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3">
                        {category.posts.map((post) => {
                            // 从文章内容中提取 LinkCard 数据
                            const content = post?.render()?.then((r) => r.html) || post?.body;
                            // 由于无法直接解析组件，这里使用简化方式
                            // 实际的链接卡片会在文章详情页显示
                            return (
                                <a href={`/posts/${post.slug}/`} class="nav-link-card">
                                    <div class="nav-link-card-inner">
                                        <div class="nav-link-icon">🔗</div>
                                        <div class="nav-link-content">
                                            <div class="nav-link-name">{post.data.title}</div>
                                            <div class="nav-link-desc">{post.data.description}</div>
                                        </div>
                                    </div>
                                </a>
                            );
                        })}
                    </div>
                </div>
            ))}
        </div>
    </div>
</MainGridLayout>

<style>
    .nav-category {
        margin-bottom: 2rem;
    }

    .nav-link-card {
        display: block;
        text-decoration: none;
        transition: all 0.3s ease;
    }

    .nav-link-card-inner {
        background: rgb(255, 255, 255);
        border-radius: 12px;
        padding: 16px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        gap: 12px;
        height: 100%;
    }

    .dark .nav-link-card-inner {
        background: rgb(45, 45, 45);
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
    }

    .nav-link-card:hover .nav-link-card-inner {
        transform: translateY(-2px);
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
    }

    .dark .nav-link-card:hover .nav-link-card-inner {
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
    }

    .nav-link-icon {
        font-size: 28px;
        line-height: 1;
        flex-shrink: 0;
        width: 44px;
        height: 44px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: rgb(245, 245, 245);
        border-radius: 10px;
    }

    .dark .nav-link-icon {
        background: rgb(55, 55, 55);
    }

    .nav-link-content {
        flex: 1;
        min-width: 0;
    }

    .nav-link-name {
        font-size: 14px;
        font-weight: 600;
        color: rgb(30, 30, 30);
        margin-bottom: 4px;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .dark .nav-link-name {
        color: rgb(230, 230, 230);
    }

    .nav-link-desc {
        font-size: 12px;
        color: rgb(140, 140, 140);
        overflow: hidden;
        text-overflow: ellipsis;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        line-height: 1.4;
    }

    .dark .nav-link-desc {
        color: rgb(160, 160, 160);
    }

    .nav-link-card:hover .nav-link-name {
        color: var(--primary);
    }

    .nav-link-card:hover .nav-link-icon {
        background: var(--primary) / 0.1;
    }
</style>
